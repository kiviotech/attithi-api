{
  "kind": "collectionType",
  "collectionName": "rooms",
  "info": {
    "singularName": "room",
    "pluralName": "rooms",
    "displayName": "room",
    "description": ""
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "room_number": {
      "type": "string"
    },
    "room_type": {
      "type": "enumeration",
      "enum": [
        "AC Rooms",
        "Non-AC Rooms"
      ]
    },
    "status": {
      "type": "enumeration",
      "enum": [
        "available",
        "occupied",
        "cleaning",
        "blocked"
      ]
    },
    "beds": {
      "type": "integer"
    },
    "room_category": {
      "type": "enumeration",
      "enum": [
        "Guest house",
        "F",
        "Yatri Niwas"
      ]
    },
    "booking_request": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "api::booking-request.booking-request",
      "inversedBy": "rooms"
    }
  }
}
